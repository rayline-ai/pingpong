{
  "_comment": [
    "The single Rayline RouterConfig for PingPong. PingPongAPI does not parse this",
    "file — Rayline consumes it. Change a brain here and nothing else changes.",
    "",
    "`model_routes` keys are ARBITRARY ALIASES, not model ids. Each agent requests",
    "its role alias and Rayline resolves it to the model below. The aliases are",
    "what keeps the two roles independently retargetable: point one at a concrete",
    "model and the other elsewhere, and nothing outside this file changes.",
    "",
    "Both default to `rayline-router`, which is not a model but the cloud-side",
    "auto-router at api.rayline.ai — it picks per request (observed choosing",
    "deepseek/deepseek-v4-flash for a trivial prompt). Note the trade: the concrete",
    "model is no longer visible in the local rld log, which reports only",
    "`selected=rayline-router`, and the reviewer and coder can land on the same",
    "model, so they may share its blind spots. Pin both to different models if that",
    "matters more to you than the routing.",
    "",
    "`routes.main` is the fallback for any request that names neither alias, and",
    "`routes.subagent` covers delegated work. Both are on `rayline-router` too, so",
    "every leg is auto-routed unless you pin it.",
    "",
    "CREDENTIALS ARE NAMED `RAYLINE_<PROVIDER>_API_KEY` INSIDE THE AGENT, AND",
    "SOMETHING ELSE IN .env. docker-compose.yml is the mapping, one line each:",
    "  RAYLINE_ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}",
    "The prefix is not decoration. `ANTHROPIC_API_KEY` already means something in",
    "these containers: Hermes will not start without one and sends it as x-api-key",
    "to the local injector, so agent.Dockerfile bakes in a deliberate placeholder.",
    "Point that name at a real key and Hermes stops failing loudly when it bypasses",
    "the router — it just talks to api.anthropic.com, ignores every route below,",
    "and bills you for it without a word. The rule that keeps the two apart: in an",
    "agent container every credential the ROUTER uses is RAYLINE_-prefixed, and an",
    "unprefixed provider key belongs to the agent runtime. `ollama-local` needs no",
    "key at all.",
    "",
    "rld ALSO SHIPS ITS OWN `anthropic`, `openai` and `openrouter` endpoints, keyed",
    "off the conventional variable names, and its startup log says which it is",
    "hiding for want of a key:",
    "  WARN hiding endpoint \"openai\" from model catalog: OPENAI_API_KEY unset",
    "That is expected here and not a fault — nothing routes to them, and the ones",
    "below are the ones these aliases name. The built-in `anthropic` is the reason",
    "to be explicit: it reads ANTHROPIC_API_KEY, which in these containers is the",
    "injector placeholder, so it looks configured and is not.",
    "",
    "TO SWITCH A ROLE TO A SPECIFIC CLOUD MODEL: replace `rayline-router` in that",
    "alias's `model` with any id from the rayline-cloud `models` list below, e.g.",
    "  \"coder-brain\": { \"endpoint\": \"rayline-cloud\", \"model\": \"gpt-5.6-terra\", ... }",
    "That also puts the real model back in the rld log.",
    "",
    "TO SWITCH A ROLE STRAIGHT TO ANTHROPIC OR OPENAI: set ANTHROPIC_API_KEY or",
    "OPENAI_API_KEY in .env, then point the alias at that endpoint, e.g.",
    "  \"reviewer-brain\": { \"endpoint\": \"anthropic-direct\", \"model\": \"claude-opus-5\", ... }",
    "  \"coder-brain\":    { \"endpoint\": \"openai-direct\", \"model\": \"gpt-5.6\", ... }",
    "and `docker compose up -d reviewer coder` so the containers pick up the key.",
    "This bypasses Rayline's routing, not Rayline: rld still speaks Anthropic to",
    "Hermes and translates, which is what `openai_chat` costs nothing to use.",
    "",
    "TO SWITCH A ROLE TO OPENROUTER: set OPENROUTER_API_KEY in .env, then",
    "  \"coder-brain\": { \"endpoint\": \"openrouter\", \"model\": \"moonshotai/kimi-k3\", ... }",
    "and `docker compose up -d coder` so the container picks up the key.",
    "",
    "TO SWITCH A ROLE TO A LOCAL MODEL: point it at the `ollama-local` endpoint,",
    "  \"coder-brain\": { \"endpoint\": \"ollama-local\", \"model\": \"qwen3.5:9b-32k\", ... }",
    "then `docker compose restart coder` so rld reloads this file. ollama speaks",
    "Anthropic natively on /v1/messages, so nothing translates.",
    "",
    "THE `-32k` SUFFIX IS LOAD-BEARING. The two unsuffixed tags above (qwen2.5-coder:7b,",
    "gemma4:26b-a4b-it-qat) are stock and will NOT call tools until you pin them the",
    "same way. ollama sizes the context window from VRAM (OLLAMA_CONTEXT_LENGTH,",
    "'default: 4k/32k/256k based on VRAM'), so a constrained host silently gets 4096",
    "tokens — well under what the harness needs. Hermes sends 19",
    "tools and a 23.6 KB system prompt; the overflow truncates the TOOL DEFINITIONS,",
    "and the model then narrates shell commands as prose instead of calling tools —",
    "which reads exactly like 'this model is too dumb'. It is not. qwen3.5:9b and",
    "qwen3.6:35b-a3b failed identically until the window was pinned. Rayline cannot",
    "fix this for you: ollama ignores `options` on its compat routes. Bake it in:",
    "  printf 'FROM qwen3.5:9b\\nPARAMETER num_ctx 32768\\n' > Modelfile",
    "  ollama create qwen3.5:9b-32k -f Modelfile",
    "Verify with `ollama ps` — the CONTEXT column must read 32768, not 4096.",
    "With that pinned, qwen3.5:9b-32k drove a full unattended round on PR #7."
  ],
  "endpoints": [
    {
      "id": "rayline-cloud",
      "protocol": "anthropic_messages",
      "base_url": "https://api.rayline.ai",
      "api_key_env": "RAYLINE_ROUTER_API_KEY",
      "auth": "api_key",
      "models": [
        "rayline-router",
        "gpt-5.6-terra",
        "claude-sonnet-5",
        "z-ai/glm-5.2"
      ]
    },
    {
      "id": "anthropic-direct",
      "protocol": "anthropic_messages",
      "base_url": "https://api.anthropic.com",
      "api_key_env": "RAYLINE_ANTHROPIC_API_KEY",
      "auth": "api_key",
      "models": [
        "claude-opus-5",
        "claude-sonnet-5",
        "claude-haiku-4-5-20251001"
      ]
    },
    {
      "id": "openai-direct",
      "protocol": "openai_chat",
      "base_url": "https://api.openai.com",
      "api_key_env": "RAYLINE_OPENAI_API_KEY",
      "auth": "bearer",
      "models": [
        "gpt-5.6",
        "gpt-5.6-mini"
      ]
    },
    {
      "id": "openrouter",
      "protocol": "anthropic_messages",
      "base_url": "https://openrouter.ai/api",
      "api_key_env": "RAYLINE_OPENROUTER_API_KEY",
      "auth": "bearer",
      "models": [
        "moonshotai/kimi-k3",
        "z-ai/glm-5.2",
        "anthropic/claude-sonnet-4.6"
      ]
    },
    {
      "id": "ollama-local",
      "protocol": "anthropic_messages",
      "base_url": "http://host.docker.internal:11434",
      "models": [
        "qwen3.5:9b-32k",
        "qwen3.6:35b-a3b-32k",
        "qwen2.5-coder:7b",
        "gemma4:26b-a4b-it-qat"
      ]
    }
  ],
  "routes": {
    "main": {
      "endpoint": "rayline-cloud",
      "model": "rayline-router",
      "router": "rayline-local"
    },
    "subagent": {
      "endpoint": "rayline-cloud",
      "model": "rayline-router",
      "router": "rayline-local"
    },
    "model_routes": {
      "reviewer-brain": {
        "endpoint": "rayline-cloud",
        "model": "rayline-router",
        "router": "rayline-local"
      },
      "coder-brain": {
        "endpoint": "rayline-cloud",
        "model": "rayline-router",
        "router": "rayline-local"
      }
    }
  }
}
